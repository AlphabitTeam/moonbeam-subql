{
  "name": "moonbeam-subquery",
  "version": "1.0.0",
  "description": "Subquery for moonbeam blockchain explorer",
  "main": "dist/index.js",
  "scripts": {
    "build": "./node_modules/.bin/subql build",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/parascan-xyz/moonbeam-subql",
  "repository": "github:parascan-xyz/moonbeam-subquery",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Alphabit Team",
  "license": "MIT",
  "devDependencies": {
    "@polkadot/api": "^7",
    "@subql/cli": "latest",
    "@subql/types": "latest",
    "moonbeam-types-bundle": "^2.0.4",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "@subql/contract-processors": "^0.6.0"
  },
  "resolutions": {
    "ipfs-unixfs": "6.0.6"
  }
}
